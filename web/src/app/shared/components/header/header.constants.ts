/** Breakpoint (px) below which the compact search placeholder is used */
export const MOBILE_BREAKPOINT = 1024;

/** Compact search placeholder for mobile viewports */
export const SEARCH_PLACEHOLDER_MOBILE = 'Search...';

/** Full search placeholder for desktop viewports */
export const SEARCH_PLACEHOLDER_DESKTOP = 'Find cars, phones, furniture...';

/** Delay (ms) before closing the search dropdown on blur */
export const SEARCH_BLUR_DELAY = 200;

/** Delay (ms) before executing logout to allow UI transitions */
export const LOGOUT_DELAY = 150;

/** Scroll threshold (px) to trigger the scrolled header state */
export const SCROLL_THRESHOLD = 10;
