version: "3.8"

services:
  mongodb:
    image: mongo:7
    restart: unless-stopped
    # Bound to the loopback interface. "27017:27017" publishes on 0.0.0.0, which
    # on any cloud host without a strict firewall is an internet-reachable
    # database — and this base file has no authentication, so that was
    # unauthenticated remote access. The backend reaches it over the compose
    # network by service name and does not need the mapping at all; it is kept,
    # narrowed, only so local tooling can still connect.
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: marketplace
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    healthcheck:
      test: >
        mongosh --eval "
          try { rs.status().ok } catch(e) { rs.initiate({ _id: 'rs0', members: [{ _id: 0, host: 'localhost:27017' }] }).ok }
        "
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.18.0
    restart: unless-stopped
    ports:
      - "127.0.0.1:9200:9200"
    environment:
      - discovery.type=single-node
      # Left off for local work only. The staging and production overlays turn it
      # on and override the healthcheck below to authenticate; without that, both
      # deployments ran an Elasticsearch that accepted any request, and the app
      # connects anonymously whenever no credentials are configured.
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    volumes:
      - es_data:/usr/share/elasticsearch/data
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    environment:
      MONGODB_URI: mongodb://mongodb:27017/marketplace?replicaSet=rs0&directConnection=true
      REDIS_HOST: redis
      ELASTICSEARCH_NODE: http://elasticsearch:9200
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy

volumes:
  mongodb_data:
  redis_data:
  es_data:
